/// 难度 3：从平均分中找出成绩最高的学生。
/// - 如果原始记录为空，返回相同的错误。
pub fn top_student<'a>(
    records: &'a [(&'a str, u32)],
) -> Result<(&'a str, f64), &'static str> {
    // 提示：可以复用前面的 `student_average` 结果。
    todo!("返回最佳学生的名字与平均分")
}
