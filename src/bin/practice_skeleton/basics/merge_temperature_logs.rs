/// 难度 3：合并主温度记录与备用记录。
/// - `primary` 拥有所有权，你可以直接修改它。
/// - `secondary` 以切片形式提供，需要按需借用或复制。
/// - 返回合并后的新向量，要求升序排列。
pub fn merge_temperature_logs(mut primary: Vec<f64>, secondary: &[f64]) -> Vec<f64> {
    // 提示：考虑使用 `extend_from_slice` 或 `sort_by`。
    todo!("合并两个数据源并返回排序后的向量")
}
